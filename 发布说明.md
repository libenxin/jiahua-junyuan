# 嘉华珺园签约监测网页发布说明

这个目录是一个纯静态网页，可以部署到任意静态托管平台，部署后别人和手机都可以通过公网链接访问。

## 文件结构

- `building-1-2-sales.html`：网页入口文件
- `Supabase操作文档.html`：第一次使用 Supabase 的图文操作文档
- `assets/project-data.js`：嘉华珺园 16 个楼栋房源数据
- `assets/charts.js`：页面交互、图表和 Supabase 云端快照对比逻辑
- `assets/supabase-config.js`：Supabase 前端只读配置
- `_shared/js/echarts.min.js`：图表依赖
- `supabase/schema.sql`：Supabase 建表和只读权限策略
- `supabase/README.md`：Supabase 接入说明

## 最简单的发布方式

### 方式一：Netlify Drop

1. 打开 `https://app.netlify.com/drop`
2. 将整个 `building-1-2-sales` 文件夹拖进去
3. 上传完成后，Netlify 会生成一个公网链接
4. 手机和别人都可以打开这个链接查看

### 方式二：Vercel

1. 新建一个静态站点项目
2. 上传整个 `building-1-2-sales` 文件夹
3. 将入口文件设置为 `building-1-2-sales.html`

### 方式三：任意服务器或对象存储

把整个 `building-1-2-sales` 文件夹上传到服务器、OSS、COS、S3 或静态网站空间即可。

## 注意

“昨日成交情况”已改为读取 Supabase 云端历史快照。所有访问者打开同一个网页时，会看到同一份最新日期和前一日对比数据，不再依赖各自浏览器的本地存储。

首次接入 Supabase 时，需要先执行 `supabase/schema.sql`，再填写 `assets/supabase-config.js` 里的 Project URL 和 anon public key。网页端只能放 anon public key，写入数据用的 service_role key 只能放在每日自动更新任务或服务端环境变量里。

## 后续每日更新规则

- 面积段已经人工校正，后续每日更新时不要重新遍历或覆盖平米数。
- 每日只需要更新 Supabase 中的项目概览签约数据和楼盘表房源交易状态；页面会自动读取最新两天数据并计算昨日成交/退房变化。
- 当前人工校正包括：87平统一为88平，138平统一为134平，173平统一为172平。

